$('#regTitle').empty().append(newcontent);

  function inc(String url){
   if(url.equals(
      var divCounter = document.body.firstElementChild.firstElementChild.firstElementChild.firstElementChild.lastElementChild;
      divCounter.innerHTML = count;
      count++;
  };